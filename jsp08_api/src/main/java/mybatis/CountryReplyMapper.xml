<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="CountryReplyMapper">
  	<insert id="insert">
  		INSERT INTO c_reply(rnum, countrycode, content, restep, relevel) 
		VALUES(cr_seq.nextval, #{countrycode}, #{content}, #{restep}, #{relevel})
  	</insert>
  
  </mapper>